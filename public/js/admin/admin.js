// Recoger el boton de crear nuevo punto

const buttonCreatePoint = document.getElementById('button-create-point');

buttonCreatePoint.addEventListener('click', () => {
    console.log('Crear nuevo punto');
});